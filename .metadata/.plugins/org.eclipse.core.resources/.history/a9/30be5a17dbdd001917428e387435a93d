package programmingExcercises;

import exceptions.CheckArgumentException;

public class ExceptionsClient {

	public static void main(String[] args) {
		question1(args);

	}

	private static void question1(String... args) {
		CheckingArgument boj = new CheckingArgument();
		try {
			boj.validateNumbers(args);
		} catch (CheckArgumentException e) {
			System.out.println(e.getMessage());
		}
	}

	private static void question2(String... args) throws CheckArgumentException {
		if (args.length <7) {
			throw new CheckArgumentException("Number of arguments can't be less than five");
		} else {

			String name=args[0];
			int[] marks=new int[args.length];
			for(int i=0;i<=args.length;i++) {
			 marks[i]=Integer.parseInt(args[i]);
			}
		}
	}

}
